# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vueblog?serverTimezone=Asia/Shanghai
    username: root
    password: lvmingji
shiro-redis:
  enabled: true
  redis-manager:
    host: 127.0.0.1:6379
project:
  env: default